# Лабораторная работа №35. Подключаем фронтенд к API: доска мемов

**ФИО:** Заставная Наталия Владимировна

**Группа:** ИСП-231

## Как запустить бэкенд

1. Перейти в папку проекта `MemesApi`
2. Запустить проект через терминал с командой `dotnet run`
3. Перейти по ссылке хоста через `ctrl + click`

## Как открыть фронтенд
1. Открыть файл `index.html`
2. Запустить через Live Server или перенести index.html в браузер

## Что изучили

|Концепция|Где используется|
|---|---|
|fetch(url)|GET-запрос к API|
|fetch(url, { method, headers, body })|POST и DELETE запросы|
|response.ok|Проверка успешности ответа
|
|response.json()| Чтение JSON из ответа|
|JSON.stringify(...)| Объект JS → JSON-строка для отправки|
|async / await|Ожидание ответа от сервера|
|try / catch / finally|Обработка ошибок при fetch|
|CORS| Разрешение запросов между разными портами|
|AddCors + UseCors|Включение CORS в ASP.NET Core — два шага|

## Итоговая таблица: что изучили в лабораторной

|Концепция|Описание|
|---|---|
|fetch(url)| GET-запрос — получить данные с сервера|
|fetch(url, { method: 'POST', ... })|POST-запрос — отправить данные|
|fetch(url, { method: 'DELETE' })|DELETE-запрос — удалить данные|
|response.ok|true если статус 200–299|
|response.json()| Прочитать JSON из тела ответа|
|JSON.stringify(...)| Превратить объект в JSON-строку|
|async / await|Дождаться ответа от сервера|
|try / catch / finally|Обработка ошибок, гарантированный блок|
|CORS| Браузер блокирует запросы между разными портами|
|AddCors + UseCors|Включение CORS — два шага, оба обязательны|
|API_URL — константа| Адрес сервера в одном месте|
|api.js — отдельный файл| Все fetch-запросы отдельно от логики страницы|
|button.disabled = true|Блокировка кнопки во время запроса|
|DevTools → Network|Смотрим запросы, ответы, заголовки CORS|